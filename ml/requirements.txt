# Installed into /home/dev/.venv inside the dev container. torch is already
# present from the image (2.14.0+cu130); it is listed without a version so pip
# leaves the working CUDA build alone.
torch
numpy
transformers>=4.57
peft>=0.14
trl>=0.12
datasets>=3.0
accelerate>=1.0
sentencepiece
protobuf
# Only needed for --load-4bit. Verified working against the cu130 torch build.
bitsandbytes
